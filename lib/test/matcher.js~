'use strict';

let Matcher = require('./../../models/matcher');

describe('matcher', function() {
	beforeEach(function() {
		this.matcher = new Matcher.Matcher();
	});

	it('can add entries', function() {
		
	});
});